# Статус выполнения плана WSDM 2027 — срез на 2026-07-08

Сводка по экспериментам основного плана (`trm_reranker_wsdm2027_plan.md`, E0–E9) и дополнения
(`trm_reranker_plan_addendum.md`, E10–E13) относительно фактического состояния кода в репозитории.

Легенда: ✅ готово · 🟡 частично · ❌ нет кода · ⬜ не начато (по плану позже)

## Эксперименты основного плана (статья 1, WSDM)

| # | Эксперимент | Дедлайн | Статус | Что есть | Чего не хватает |
|---|---|---|---|---|---|
| E0 | Аудит выравнивания рук (К1–К4) | 11 июля ⚠️ | 🟡 ~85% | Аудит по коду выполнен, см. [e0_alignment_audit_2026-07-08.md](e0_alignment_audit_2026-07-08.md). К1 (блоки) — выровнены. К2 — найдено расхождение (one-step gradient в TRM; флаг абляции `full_backprop` реализован) + конфаунд batch size 512 vs 256. FLOPs измерены (`scripts/measure_footprint.py`): deep-20L FLOP-matched к TRM подтверждён (34.93 GFLOPs/пара у обоих) | Сверка с `training_config.json` реальных прогонов deep-арма (batch, число слоёв); вердикт «переобучаем / не переобучаем» |
| E1 | Починка BERT cross-encoder baseline | 18 июля | ❌ | Диагноз подтверждён по коду: в обоих BERT-ноутбуках `FREEZE_ENCODER = True`; ноутбука полного файнтюна не существует — 0.15 это frozen-энкодер | Тренировочная рука с полным файнтюном (LR 2–3e-5, warmup, 1M триплетов); чек-лист коллеги (requires_grad, параметры в оптимизаторе, официальные triples); санити-чек eval-скрипта готовой моделью |
| E2 | Эквивалентность: сиды, значимость, память | 27 июля / 3 авг | ❌ | Одиночные прогоны TRM и deep есть (метрики ±равные — по плану). Eval пишет метрики и BM25/candidate-order baseline | Мультисид-оркестрация (сейчас `SEED=13` — константа); paired bootstrap + t-test (в коде нет ranx/ir_measures/scipy); выгрузка per-query метрик; единый скрипт замера params (body/total) / checkpoint fp16 / peak memory / latency / GFLOPs |
| E3 | Weight-tied deep (ALBERT-арм) | 5 августа | ❌ | Vanilla-класс с настраиваемой глубиной — основа готова | Вариант с cross-layer tying (без zH/zL и инжекции); конфиг + прогоны 3M, ≥2 сида |
| E4 | Кривая глубины рекурсии (H/L) | 10 августа | 🟡 | Модель параметризована H_cycles/L_cycles — любая точка сетки запускается сменой конфига | Оркестрация сетки H∈{1,2}×L∈{2,4,8}; измеренные GFLOPs как ось X; сводный график |
| E5 | Абляция ре-инжекции входа | 10 августа | ❌ | — | Флаг `disable_input_injection` в TRM (тривиально); 1–2 прогона |
| E6 | TREC DL 2019/2020 | 8 августа | ❌ | Запись TREC run формата есть только в monoT5-ноутбуке | Inference-скрипт всех рук на DL19/20 (ir_datasets/ir_measures); nDCG@10 + CI |
| E7 | BEIR-подмножество (опц.) | 14 августа | ⬜ | — | Решение go/no-go до 1 августа; инференс на top-100 BM25 |
| E8 | Hard negatives + multi-negative loss (опц.) | go/no-go 20 июля | ⬜ | Даталоадер только pairwise triples | Загрузка msmarco-hard-negatives; InfoNCE/gBCE loss |
| E9 | MLM-претрейн (фон, не блокер) | — | ⬜ | — | Вся ветка; для WSDM — одно предложение в future work |

## Эксперименты дополнения (статья 2 + заимствования в WSDM)

| # | Эксперимент | Куда | Статус | Комментарий |
|---|---|---|---|---|
| E10 | Адаптивный halting | Статья 2 | 🟡 ~60% модели | ACT-механика в TRM реализована (q_head, halt_exploration_prob, no_ACT_continue), но нигде не включена: `HALT_MAX_STEPS=1` во всех конфигах, включая halt_exper. Нет промежуточного скоринга по шагам, калибровки порога, Парето |
| E11 | Кривая Парето качество-latency | Статья 2 | ❌ | Строится из E4+E10+E12 |
| E12a | LoRA / linear-head конкуренты | **WSDM** | 🟡 | Linear-probe фактически есть: `bert_encoder_only_ablation` = frozen BERT + scoring head. LoRA (peft) в коде нет |
| E12b | DeeBERT-style early exit | Статья 2 | ❌ | — |
| E12c | GRU/LSTM-голова | Статья 2 | ❌ | — |
| E13 | Error analysis (глубина vs трудность) | Статья 2 | ❌ | — |

## Заострения коллеги

| Пункт | Статус | Комментарий |
|---|---|---|
| Протокол latency (median+p95, warmup, per-config) | 🟡 ~80% | В `all_architectures_latency` уже есть warmup + p50/p95. Нужно: единый скрипт для всех рук, замер per-config (закрыть «скопированные 4.80»), среднее число шагов рекурсии для адаптивной модели |
| Строка BM25 во всех таблицах | ✅ код / 🟡 таблицы | Eval уже считает candidate-order baseline; осталось протаскивать в таблицы статьи |
| Чек-лист отладки BERT (5 пунктов) | ❌ | Встроен в задачу E1; особенно санити-чек eval-скрипта готовой моделью |

## Инфраструктура (рефакторинг репозитория)

Состояние на утро 2026-07-08: 12 Jupyter-ноутбуков, дублирование общего кода между
тренировочными ноутбуками 90–99% (например, 2009 из 2013 уникальных строк `pure_trm_reranker`
совпадают с `halt_exper`). Нет менеджера зависимостей, конфигов, тестов, значимости, FLOPs.

План рефакторинга (фазы) — см. CHANGELOG и README после выполнения:

- [x] Фаза 0: аудит E0 по коду, статус плана (этот документ)
- [x] Фаза 1 (выполнена 2026-07-08): uv + пакет `src/trm_reranker` (models/data/training/evaluation/benchmarks), configs, scripts (train/evaluate/measure_footprint/significance), тесты (20 passed)
- [x] Фаза 3 (выполнена 2026-07-08): ноутбуки в `notebooks/legacy/`, README, docs/, CHANGELOG
- [ ] Фаза 2 (наука, по дедлайнам): прогоны E1 (конфиг готов) → мультисид + значимость E2 (инструменты готовы) → tied-арм E3 и абляция E5 (конфиги/флаги готовы) → внешний eval DL19/20 (E6, кода нет) → LoRA (E12a, кода нет) → hard negatives (E8, кода нет)
- [ ] Parity-прогон: загрузить легаси-чекпоинт в новую модель (`TRM_LEGACY_CHECKPOINT=... pytest tests/test_parity.py`) — обязательный шаг перед заменой старых чисел
- [ ] Портирование data_prep-ноутбуков в скрипты (не блокер: текущие кэши уже собраны)

## Ближайшие развилки, требующие решения (не кода)

1. **Вариант C (две статьи) или A (одна)** — открытый вопрос из addendum; влияет на то, ставить ли E10/E12b-c в очередь фоном уже сейчас.
2. **Вердикт E0 после сверки batch size** — если deep-арм реально обучался с batch 256 против 512 у TRM, нужно решение о переобучении (риск ~недели компьюта, каскад на сроки E2).
3. Ответственные за E2/E3 (обучение) и E6/E7 (инференс) — по плану закрепить до 12 июля.
